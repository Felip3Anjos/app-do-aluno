name: mobile
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.1
  provider: ^6.1.2
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  shared_preferences: ^2.2.3
  audioplayers: ^6.0.0
  
  # --- NOVO PACOTE ADICIONADO PARA A PÁGINA DE VESTIBULARES ---
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  dio: ^5.3.3 # Ou a versão mais recente
  path_provider: ^2.1.1 # Ou a versão mais recente
  permission_handler: ^11.0.1 # Ou a versão mais recente

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/sounds/

